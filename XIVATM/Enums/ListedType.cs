namespace XIVATM.Structs;

// Determines if the whitelisted or blacklisted player should be globally banned on their service account or if only the character should be banned.
public enum ListedType
{
    ServiceAccount,
    Character,
}
